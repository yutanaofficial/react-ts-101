var shape = "circle";
let radius = 10;
const PI = 3.14;

if (radius > 0) {
  let area = PI * radius * radius;
  console.log(`Area of ${shape} is ${area}`);
}

console.log(`Value of shape is ${shape}`);
console.log(`Value of PI is ${PI}`);
console.log(`Value of radius is ${radius}`);
// console.log(`Value of area is ${area}`);
